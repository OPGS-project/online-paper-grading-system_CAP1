import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => <ReactLoading type="spin" color="#ccc" height={20 % Loading} width={20 % Loading} />;

export default Loading;
